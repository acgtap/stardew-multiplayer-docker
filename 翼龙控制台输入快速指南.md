# 翼龙面板控制台输入 - 快速指南

## 问题

在翼龙面板中输入命令，SMAPI 没有反应？

## 原因

SMAPI 默认运行在 `xterm` 内部，翼龙控制台的输入无法传递到 xterm。

## 快速解决方案

### 方案 1：启用控制台输入模式（推荐用于调试）

1. 重新构建并推送镜像：
```bash
cd D:\Documents\GitHub\stardew-multiplayer-docker
docker build --no-cache -t ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest -f docker/Dockerfile docker/
docker push ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

2. 更新翼龙 Egg（导入 `egg-stardew-valley.json`）

3. 在翼龙节点更新镜像：
```bash
docker rmi ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
docker pull ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

4. 在服务器的 **Startup** 标签中：
   - 找到 **Enable Console Input**
   - 设置为 `true`
   - 重启服务器

5. 现在可以在翼龙控制台直接输入 SMAPI 命令！

**注意**：启用后 VNC 图形界面可能不可用，适合只需要命令行管理的场景。

### 方案 2：使用 RemoteControl Mod（推荐用于日常管理）

如果您需要图形界面，保持 `ENABLE_CONSOLE_INPUT=false`，使用 RemoteControl mod：

1. 确保启用 `Enable Remote Control = true`（在 Startup 标签）
2. 通过 VNC 连接到服务器
3. 在游戏聊天中输入命令（带 `/` 前缀）：
   - `/help` - 显示可用命令
   - `/time <时间>` - 设置游戏时间
   - `/money <数量>` - 设置金钱

这样您可以在游戏内管理服务器，同时保持图形界面可用。

## 两种模式对比

| 特性 | xterm 模式（默认） | 控制台输入模式 |
|------|-------------------|---------------|
| 翼龙控制台输入 | ❌ 不可用 | ✅ 可用 |
| VNC 图形界面 | ✅ 完整可用 | ⚠️ 可能不可用 |
| 稳定性 | ✅ 更稳定 | ⚠️ 可能不稳定 |
| 适用场景 | 日常运行、初次设置 | 调试、命令行管理 |
| 设置值 | `ENABLE_CONSOLE_INPUT=false` | `ENABLE_CONSOLE_INPUT=true` |

## 推荐配置

**新手/大多数用户**：
- `ENABLE_CONSOLE_INPUT=false`（使用 xterm 模式）
- `Enable Remote Control=true`（启用游戏内命令）
- 通过 VNC 访问和管理

**高级用户/服务器调试**：
- `ENABLE_CONSOLE_INPUT=true`（启用控制台输入）
- 直接在翼龙面板执行 SMAPI 命令
- 不需要 VNC

## 常用命令示例

启用控制台输入后，您可以直接在翼龙控制台输入：

```
help                           # 显示所有命令
debug                          # 切换调试模式
world_clear                    # 清理地图垃圾
player_setmoney 999999         # 设置金钱
player_setlevel farming 10     # 设置农业技能到10级
reload_i18n                    # 重新加载语言文件
```

---

详细文档请查看：`翼龙控制台命令输入指南.md`

