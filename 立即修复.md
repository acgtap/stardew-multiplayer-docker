# 🚀 立即修复 - Pterodactyl 错误

## 您遇到的错误

```
✗ vncpasswd: command not found
✗ SMAPI must be placed in the Stardew Valley game folder
```

## ✅ 已修复！

代码已更新，修复了：
1. VNC 密码配置错误
2. SMAPI 游戏路径错误
3. 只读文件系统错误

## 📋 立即执行（3 步）

### 第 1 步：重新构建镜像

在项目目录打开 PowerShell：

```powershell
cd D:\Documents\GitHub\stardew-multiplayer-docker

# 清理缓存
docker builder prune -f

# 构建镜像（需要 5-10 分钟）
docker build --no-cache -t ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest -f docker/Dockerfile docker/
```

### 第 2 步：推送镜像（可选）

如果您有自己的镜像仓库：

```powershell
docker push ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

### 第 3 步：在翼龙节点更新

SSH 到您的翼龙节点服务器：

```bash
# 删除旧镜像
docker rmi ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest

# 拉取新镜像
docker pull ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

然后在翼龙面板重启服务器。

## ✨ 成功标志

重启后应该看到：

```
✅ ==> Running in Pterodactyl mode - bypassing s6-overlay
✅ ==> Pterodactyl deployment detected - using /home/container for saves
✅ ==> Skipping mod configuration (read-only filesystem in Pterodactyl mode)
✅ ==> VNC password configured
✅ ==> Started Xvfb (PID: xxx) at 1200x900
✅ ==> Started x11vnc on port 5900
✅ -- SMAPI Log: Starting
```

**不应该看到**：
```
❌ vncpasswd: command not found
❌ SMAPI must be placed in the Stardew Valley game folder
❌ Read-only file system
❌ Permission denied
```

## ❓ 仍有问题？

查看详细文档：
- `翼龙面板修复步骤.md` - 完整修复指南
- `QUICK_FIX.md` - 快速参考

---

**修复日期**：2025-10-28（最新更新）

