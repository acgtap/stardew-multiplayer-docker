# 翼龙面板控制台命令输入指南

## 问题说明

在翼龙面板中，默认情况下您**无法直接向SMAPI输入命令**，这是因为：

1. SMAPI 运行在 `xterm` 终端内部
2. 翼龙面板的控制台输入无法传递到 xterm 进程
3. 这是为了保持游戏窗口的稳定性

## 解决方案

我们添加了一个新的环境变量 `ENABLE_CONSOLE_INPUT`，它提供两种运行模式：

### 模式 1：xterm 模式（默认，推荐）

**设置**：`ENABLE_CONSOLE_INPUT=false`（默认值）

**特点**：
- ✅ 游戏窗口稳定，通过 VNC 可以看到完整的图形界面
- ✅ 适合大多数用户
- ❌ 无法从翼龙控制台直接输入命令
- 🔧 需要通过 VNC 访问游戏，或使用游戏内 RemoteControl mod

**适用场景**：
- 您主要通过 VNC 管理服务器
- 您不需要频繁在控制台输入命令
- 您希望游戏运行更稳定

### 模式 2：控制台输入模式（高级）

**设置**：`ENABLE_CONSOLE_INPUT=true`

**特点**：
- ✅ 可以直接在翼龙面板控制台输入 SMAPI 命令
- ✅ 可以看到 SMAPI 的完整输出
- ⚠️ 游戏窗口可能不稳定或无法正常显示
- ⚠️ VNC 可能只显示黑屏或终端文本

**适用场景**：
- 您需要频繁输入 SMAPI 命令（如 `debug`, `world_clear` 等）
- 您习惯使用命令行管理服务器
- 您不需要通过 VNC 访问图形界面
- 您在进行服务器调试或开发

## 使用步骤

### 步骤 1：重新构建 Docker 镜像

```bash
cd D:\Documents\GitHub\stardew-multiplayer-docker

# 清除构建缓存
docker builder prune -f

# 重新构建镜像
docker build --no-cache -t ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest -f docker/Dockerfile docker/

# 推送到仓库
docker push ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

### 步骤 2：更新翼龙 Egg 配置

1. 登录翼龙管理面板
2. 进入 **Admin** → **Nests** → 找到 Stardew Valley Egg
3. 点击 **Import Egg**
4. 上传更新后的 `egg-stardew-valley.json`
5. 勾选 **"覆盖现有 Egg"**
6. 保存

### 步骤 3：在翼龙节点更新镜像

SSH 到您的翼龙节点服务器：

```bash
# 删除旧镜像
docker rmi ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest

# 拉取新镜像
docker pull ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

### 步骤 4：配置服务器

1. 进入您的 Stardew Valley 服务器
2. 点击 **Startup** 标签
3. 找到 **Enable Console Input** 变量
4. 根据需要选择：
   - `false`（默认）：使用 xterm 模式，需要通过 VNC 访问
   - `true`：启用控制台输入，可以在翼龙面板直接输入命令
5. 保存并重启服务器

### 步骤 5：验证

**xterm 模式（ENABLE_CONSOLE_INPUT=false）**：
启动后控制台显示：
```
==> 使用 xterm 模式 - 控制台输入不可用，使用 VNC 或启用 ENABLE_CONSOLE_INPUT
==> 启动 Xvfb (PID: xxx) at 1200x900
==> 启动 x11vnc on port 5900
SMAPI ready
```

**控制台输入模式（ENABLE_CONSOLE_INPUT=true）**：
启动后控制台显示：
```
==> 启用控制台输入模式 - 您可以在翼龙面板输入SMAPI命令
Stardew Valley 1.6.x (build xxxx)
SMAPI 4.x.x for Stardew Valley 1.6.x on Unix
Mods loaded: ...
> 
```

现在您可以直接输入命令，例如：
- `help` - 显示帮助
- `debug` - 切换调试模式
- `reload_i18n` - 重新加载语言文件
- `world_clear` - 清理世界物品

## 常用 SMAPI 命令

### 基础命令
- `help` - 显示所有可用命令
- `help <command>` - 显示特定命令的帮助
- `reload_i18n` - 重新加载翻译文件

### 调试命令
- `debug` - 切换调试模式
- `world_clear` - 清理地图上的垃圾物品
- `player_setmoney <amount>` - 设置玩家金钱
- `player_setlevel <skill> <level>` - 设置技能等级

### Mod 命令
不同的 mod 会添加不同的命令，使用 `help` 查看完整列表。

常见的 mod 命令示例：
- `save` - 立即保存游戏
- `nextsleep` - 跳到下一天（需要 mod 支持）
- `rc` - RemoteControl mod 命令（如果启用）

## 注意事项

1. **控制台输入模式的限制**：
   - 游戏图形界面可能无法正常工作
   - VNC 访问可能只显示终端界面
   - 如果需要进行游戏操作（如创建农场），请使用 xterm 模式

2. **推荐工作流程**：
   - **初次设置**：使用 xterm 模式（`ENABLE_CONSOLE_INPUT=false`）
   - 通过 VNC 创建农场并配置好游戏
   - **日常管理**：根据需要切换到控制台输入模式（`ENABLE_CONSOLE_INPUT=true`）
   - 如需修改游戏设置，再切换回 xterm 模式

3. **切换模式**：
   - 随时可以在 Startup 标签修改 `ENABLE_CONSOLE_INPUT` 值
   - 修改后需要重启服务器才能生效
   - 不会影响存档文件

## 替代方案：RemoteControl Mod

如果您不想启用控制台输入模式，也可以使用 **RemoteControl** mod：

1. 确保在 Startup 中启用了 `Enable Remote Control` = `true`
2. 通过 VNC 进入游戏
3. 在游戏聊天中输入命令（带 `/` 前缀），例如：
   - `/help` - 显示帮助
   - `/time <time>` - 设置时间
   - `/money <amount>` - 设置金钱

这样您可以在玩游戏的同时管理服务器，无需在控制台和 VNC 之间切换。

## 故障排除

### 问题：启用控制台输入后看不到命令提示符

**原因**：SMAPI 可能正在加载 mod 或显示启动信息

**解决方案**：
- 等待 SMAPI 完全加载（看到 "SMAPI ready" 消息）
- 按 Enter 键，应该会显示 `>` 提示符
- 如果仍然没有响应，检查控制台是否有错误信息

### 问题：输入命令后没有反应

**原因**：可能 SMAPI 进程没有正确启动

**解决方案**：
1. 检查控制台是否显示 "SMAPI ready"
2. 查看是否有错误日志
3. 尝试重启服务器
4. 切换回 xterm 模式验证游戏是否能正常运行

### 问题：想同时使用控制台和 VNC

**答案**：目前不支持同时使用，必须选择其中一种模式

**推荐方案**：
- 保持 xterm 模式（`ENABLE_CONSOLE_INPUT=false`）
- 使用 RemoteControl mod 在游戏内执行命令
- 这样既可以通过 VNC 看到图形界面，也可以执行管理命令

---

**更新日期**：2025-10-28  
**版本**：1.0

