# 翼龙面板 s6-overlay 错误完整修复步骤

## 错误信息
```
s6-hiercopy: fatal: unable to copy hierarchy from /etc/s6/init/env to /var/run/s6/env-stage1: No such file or directory
```

## 问题根源

基础镜像 `jlesage/baseimage-gui` 的 ENTRYPOINT 会在容器启动时运行 s6-overlay 初始化，在翼龙（Pterodactyl）环境中由于文件系统限制导致失败。

## 解决方案

### 步骤 1：重新构建镜像（必需）

在项目根目录执行：

```bash
cd D:\Documents\GitHub\stardew-multiplayer-docker

# 清除旧的构建缓存
docker builder prune -f

# 重新构建镜像（不使用缓存）
docker build --no-cache -t ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest -f docker/Dockerfile docker/

# 查看镜像是否成功构建
docker images | grep stardew
```

### 步骤 2：推送到镜像仓库

```bash
# 推送到远程仓库
docker push ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

### 步骤 3：在翼龙面板中清除镜像缓存

**方法 A：通过翼龙面板**
1. 停止服务器
2. 在服务器的 **Settings** 中，找到 **Reinstall Server** 选项
3. 点击重新安装（这会清除本地镜像缓存）

**方法 B：直接在节点服务器上删除镜像**

SSH 到您的翼龙节点服务器，执行：

```bash
# 查找容器
docker ps -a | grep stardew

# 停止并删除容器（替换为实际的容器ID）
docker stop <container_id>
docker rm <container_id>

# 删除旧镜像
docker rmi ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest

# 手动拉取新镜像
docker pull ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest

# 验证新镜像的摘要值
docker inspect ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest | grep -A 5 RepoDigests
```

### 步骤 4：更新翼龙 Egg 配置

1. 进入翼龙管理面板：**Admin** → **Nests**
2. 找到 Stardew Valley Egg
3. 点击 **Import Egg** 按钮
4. 上传更新后的 `egg-stardew-valley.json`
5. 勾选 **"覆盖现有 Egg"** 选项
6. 保存

### 步骤 5：重启服务器

返回服务器面板，点击 **Restart**

## 验证修复

重启后，控制台应该显示：

```
==> Running in Pterodactyl mode - bypassing s6-overlay
Pterodactyl deployment detected - using /home/container for saves
==> Starting VNC services for Pterodactyl...
==> Started Xvfb (PID: xxx) at 1200x900
==> Started x11vnc on port 5900
==> Started Web VNC on port 5800
==> Game server port: 24642
```

## Dockerfile 关键修改

本次修复的关键点：

1. **创建所需目录**：
```dockerfile
mkdir -p /tmp/s6-runtime/env-stage1 /tmp/s6-runtime/env-stage2
mkdir -p /etc/s6/init/env
```

2. **覆盖 ENTRYPOINT**：
```dockerfile
ENTRYPOINT []
CMD ["/bin/bash", "/startapp.sh"]
```

这样可以绕过基础镜像的 s6-overlay ENTRYPOINT。

## 故障排除

### 问题：仍然看到相同错误

**可能原因 1：镜像没有真正更新**

检查镜像摘要值：
```bash
docker inspect ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest | grep Id
```

将输出的 ID 与之前的对比，如果相同说明镜像没有更新。

**解决方案**：
```bash
# 强制删除并重新拉取
docker rmi -f ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
docker pull --no-cache ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

**可能原因 2：翼龙使用的是旧容器**

```bash
# 查看容器创建时间
docker ps -a | grep stardew

# 如果创建时间早于镜像重建时间，删除容器
docker rm -f <container_id>
```

### 问题：镜像构建失败

```bash
# 检查 Docker 守护进程
docker info

# 清理所有构建缓存
docker system prune -a

# 重新构建
docker build --no-cache -f docker/Dockerfile docker/ -t ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest
```

### 问题：无法推送到镜像仓库

```bash
# 登录镜像仓库
docker login ghcr.mirrorify.net

# 或者使用其他仓库
docker tag ghcr.mirrorify.net/acgtap/stardew-multiplayer-docker:latest your-registry/stardew:latest
docker push your-registry/stardew:latest
```

然后在翼龙 Egg 配置中更新镜像地址。

## 检查清单

- [ ] 已修改 Dockerfile（包含 ENTRYPOINT[] 和目录创建）
- [ ] 已清除 Docker 构建缓存
- [ ] 已使用 --no-cache 重新构建镜像
- [ ] 已推送新镜像到仓库
- [ ] 已在翼龙节点删除旧镜像
- [ ] 已拉取新镜像
- [ ] 已验证镜像摘要值已更改
- [ ] 已更新翼龙 Egg 配置
- [ ] 已重启服务器
- [ ] 控制台输出正常，无 s6-overlay 错误

## 技术说明

### 为什么需要 ENTRYPOINT []？

基础镜像 `jlesage/baseimage-gui:debian-11` 有一个 ENTRYPOINT 指向 s6-overlay 的初始化脚本。在翼龙环境中，修改 Egg 的 `startup` 字段只能改变 CMD，无法覆盖 ENTRYPOINT。

通过在 Dockerfile 中设置 `ENTRYPOINT []`，我们清空了继承的 ENTRYPOINT，让容器直接运行我们的脚本，绕过 s6-overlay。

### 为什么仍然创建 s6 目录？

即使绕过 s6-overlay，基础镜像中可能还有其他组件依赖这些目录。创建这些目录可以避免潜在的错误。

---

**最后更新**：2025-10-28  
**关键**：必须重新构建镜像并清除缓存！

