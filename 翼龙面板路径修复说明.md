# 翼龙面板（Pterodactyl）路径修复说明

## 问题描述

在翼龙面板环境下运行时，出现以下错误：
```
Failed to create /config/xdg/cache for shader cache (No such file or directory)---disabling.
```

同时 SMAPI 日志一直在等待日志文件出现。

## 问题原因

1. **XDG 环境变量未设置**：游戏需要知道在哪里创建缓存、配置等目录，但没有设置 XDG 环境变量
2. **路径冲突**：游戏尝试访问 `/config/xdg/cache`，但在翼龙面板模式下应该使用 `/home/container`
3. **只读文件系统**：`/data` 目录可能是只读的，无法直接修改游戏文件和模组

## 修复内容

### 1. XDG 环境变量设置（第 56-60 行，68-72 行）

在翼龙面板模式下：
```bash
export XDG_CONFIG_HOME="/home/container/xdg/config"
export XDG_DATA_HOME="/home/container/xdg/data"
export XDG_CACHE_HOME="/home/container/xdg/cache"
export XDG_STATE_HOME="/home/container/xdg/state"
```

在标准 Docker 模式下：
```bash
export XDG_CONFIG_HOME="/config/xdg/config"
export XDG_DATA_HOME="/config/xdg/data"
export XDG_CACHE_HOME="/config/xdg/cache"
export XDG_STATE_HOME="/config/xdg/state"
```

### 2. 创建所有必要目录（第 75-81 行）

```bash
mkdir -p "$SAVE_DIR"
mkdir -p "$LOG_DIR"
mkdir -p "$XDG_CONFIG_HOME"
mkdir -p "$XDG_DATA_HOME"
mkdir -p "$XDG_CACHE_HOME"
mkdir -p "$XDG_STATE_HOME"
```

### 3. 翼龙面板模式下复制游戏文件（第 93-108 行）

在翼龙面板模式下，将游戏文件从只读的 `/data` 目录复制到可写的 `/home/container` 目录：

```bash
if [ "$PTERODACTYL_MODE" = true ]; then
  CONTAINER_GAME_DIR="$HOME/Stardew"
  
  if [ ! -d "$CONTAINER_GAME_DIR/Stardew Valley" ]; then
    echo "==> Copying game files to $CONTAINER_GAME_DIR..."
    mkdir -p "$CONTAINER_GAME_DIR"
    cp -r "/data/Stardew/Stardew Valley" "$CONTAINER_GAME_DIR/"
    echo "==> Game files copied successfully"
  fi
  
  export GAME_DIR="$CONTAINER_GAME_DIR/Stardew Valley"
fi
```

### 4. 在可写目录中配置模组（第 110-136 行）

在翼龙面板模式下，在 `/home/container/Stardew/Stardew Valley/Mods/` 中配置模组，而不是只读的 `/data` 目录。

### 5. 更新游戏启动脚本（第 232-270 行）

- 使用动态的 `GAME_DIR` 变量（翼龙面板模式下指向 `/home/container/Stardew/Stardew Valley`）
- 在启动脚本中传递所有 XDG 环境变量

```bash
export HOME=$HOME
export XDG_CONFIG_HOME=$XDG_CONFIG_HOME
export XDG_DATA_HOME=$XDG_DATA_HOME
export XDG_CACHE_HOME=$XDG_CACHE_HOME
export XDG_STATE_HOME=$XDG_STATE_HOME
```

## 预期效果

修复后，在翼龙面板模式下：

1. ✅ 所有数据（存档、日志、缓存、模组配置）都存储在 `/home/container` 中
2. ✅ 游戏文件和模组都在可写目录 `/home/container/Stardew/Stardew Valley` 中运行
3. ✅ 不再出现 "Failed to create /config/xdg/cache" 错误
4. ✅ SMAPI 日志正常生成在 `/home/container/xdg/config/StardewValley/ErrorLogs/SMAPI-latest.txt`
5. ✅ 模组可以正常读写配置文件

## 目录结构

翼龙面板模式下的目录结构：
```
/home/container/
├── Stardew/
│   └── Stardew Valley/          # 游戏文件（从 /data 复制）
│       ├── Mods/                # 模组目录（可读写）
│       ├── StardewModdingAPI    # SMAPI 可执行文件
│       └── ...
├── xdg/
│   ├── config/
│   │   └── StardewValley/
│   │       ├── ErrorLogs/       # SMAPI 日志
│   │       └── Saves/           # 不使用，实际存档在下面
│   ├── data/                    # 游戏数据
│   ├── cache/                   # 着色器缓存等
│   └── state/                   # 游戏状态
├── Saves/                       # 实际的存档目录
├── x11vnc.log                   # VNC 日志
└── start_game.sh                # 游戏启动包装脚本
```

## 使用方法

1. 重新构建 Docker 镜像：
```bash
docker build -t stardew-multiplayer:latest -f docker/Dockerfile .
```

2. 在翼龙面板中使用新镜像

3. 启动容器后，所有数据都会自动存储在 `/home/container` 中

## 注意事项

- **首次启动**：第一次启动会复制游戏文件，可能需要几秒到几十秒，取决于文件大小
- **后续启动**：已复制的文件不会重复复制，启动速度正常
- **数据持久化**：确保翼龙面板正确挂载 `/home/container` 卷以保存数据

## 故障排除

### 问题：提示 "No such file or directory" 错误

如果看到类似以下错误：
```
bash: line 1: /home/container/Stardew/Stardew: No such file or directory
```

**原因**：路径中的空格没有被正确处理，或者之前有旧的复制导致目录结构错误。

**解决方法**：

1. **清理旧的游戏文件**（如果是升级或重新部署）：
   - 在翼龙面板控制台执行：
   ```bash
   rm -rf /home/container/Stardew
   ```
   - 或者在翼龙面板文件管理器中删除 `Stardew` 目录

2. **重启容器**：
   - 脚本会自动重新复制游戏文件到正确的位置

3. **检查目录结构**：
   - 正确的结构应该是：`/home/container/Stardew/Stardew Valley/`
   - 而不是：`/home/container/Stardew/Stardew Valley/Stardew Valley/`

4. **查看调试输出**：
   - 启动时会显示：
     - 源目录路径
     - 目标目录路径
     - 复制状态
     - 游戏目录验证结果

### 问题：游戏文件一直重复复制

如果每次启动都在复制游戏文件：

**原因**：游戏目录没有被正确持久化。

**解决方法**：
- 确保翼龙面板中 `/home/container` 目录被正确挂载为持久卷
- 检查翼龙面板的卷配置

### 问题：SMAPI 日志一直在等待

如果看到：
```
-- SMAPI Log: Waiting for log to appear at /home/container/xdg/config/StardewValley/ErrorLogs/SMAPI-latest.txt
```

**原因**：游戏没有成功启动，无法生成日志文件。

**解决方法**：
1. 检查上面的输出，看游戏是否成功复制和验证
2. 检查 VNC 连接，看游戏是否在图形界面中启动
3. 查看完整的启动日志，找出错误信息

